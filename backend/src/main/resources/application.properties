spring.application.name=backend
# MySQL Connection Settings
spring.datasource.url=jdbc:mysql://localhost:3306/advanced_train_db
spring.datasource.username=root
# Change this to your actual MySQL password!
spring.datasource.password=Arjun3115
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Settings
# 'none' ensures Spring doesn't accidentally delete or overwrite our complex SQL tables
spring.jpa.hibernate.ddl-auto=none
# 'true' prints the SQL queries in the terminal so you can see what Java is doing
spring.jpa.show-sql=true